# PRACTICE PROJECT

Learning GIT and PYTHON Foundation

## GIT COMMANDS:

```
git config --global user.name
git config --global user.name "your_name"
git config --global user.email "your_email@example.com"
git init

git status
git add .
git commit -m "feat:Initial Setup"

git remote add origin https://github_address
git push origin master
git pull origen master

```

## PYTHON Foundation

## 1 Dars DARSIMIZ REJASI

```
DARSIMIZ REJASI
-Pythonning o'ziga xos jozibasi haqida
-Numberlarni o'rganamiz
-String hagida mushohada qilamiz
-Booleanlarni o'rganamiz
```
